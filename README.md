# Ada-2022-Directories-Hierarchical-File-Names

Ada.Directories.Hierarchical_File_Names.

Wave 26 No SPARK. Classifies and decomposes a relative hierarchical file name.

Build and test with `make && make test`.
